---
title: "IOI 2016: Bug"
---
by creating a single element set for each number from $[0, n / 2)$, we can determine whether $q_i = p^(-1)_i$ has the highest bit on or off

then we can create two element sets, where each element has the second bit off

we can't do that many checks though...
