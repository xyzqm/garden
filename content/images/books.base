filters:
  and:
    - file.hasTag("book")
    - file.name != "book"
views:
  - type: cards
    name: Cards
    sort:
      - property: rating
        direction: DESC
    image: note.image
    cardSize: 200
